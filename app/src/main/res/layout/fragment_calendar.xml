<?xml version="1.0" encoding="utf-8"?>
<!-- CalendarFragment Layout -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".ui.start.LogInFragment">

    <com.prolificinteractive.materialcalendarview.MaterialCalendarView
        android:id="@+id/calendarView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:paddingBottom="16dp"
        android:paddingHorizontal="16dp"
        app:mcv_tileHeight="48dp"
        app:mcv_calendarMode="month"
        app:mcv_arrowColor="?attr/colorOnSurface"
        android:theme="@style/Widget.SKKUHelper.MaterialCalendarView"
        app:mcv_dateTextAppearance="@style/Widget.SKKUHelper.MaterialCalendarView.DateText"
        app:mcv_headerTextAppearance="@style/Widget.SKKUHelper.MaterialCalendarView.HeaderText"
        app:mcv_weekDayTextAppearance="@style/Widget.SKKUHelper.MaterialCalendarView.WeekText"/>

    <com.google.android.material.divider.MaterialDivider
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingVertical="16dp"
        android:clipToPadding="false"
        android:scrollbars="none" />

</LinearLayout>